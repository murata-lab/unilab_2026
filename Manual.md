# UNILAB 2026：企画コード提出マニュアル

このマニュアルは、自分のパソコンにある企画コードを研究室のGitHubリポジトリへアップロードするための手順です。

研究室リポジトリ：`https://github.com/murata-lab/unilab_2026`

## 誰が何をするか

このマニュアルには、2種類の役割があります。

### 提出する学生

自分のパソコンで、clone、ブランチ作成、コード追加、commit、push、Pull Request作成までを行います。**手順1〜9は、提出する学生が行います。**

### リポジトリ管理者

学生が作成したPull Requestの内容を確認し、問題がなければ `main` へmergeします。**「管理者が行う操作」の章だけを管理者が行います。**

```text
【提出する学生】clone → ブランチ作成 → コード追加 → commit → push → Pull Request作成
                                                                    ↓
【管理者】                                                   内容確認 → merge
```

## 全体の形

このリポジトリでは、企画ごとにディレクトリを分けて管理します。

```text
unilab_2026/
├── 企画A/
├── 企画B/
├── 企画C/
└── UNILAB_2026_利用マニュアル.pdf
```

今年企画を担当した学生が、自分の企画名のディレクトリを作成して完成済みコードを入れます。複数人が同時に提出しても影響しにくいよう、学生ごとに作業用ブランチを作り、Pull Request経由で提出します。`main` へ直接pushしないでください。

# 提出する学生が行う操作

ここから手順9までは、企画コードを提出する学生本人が行ってください。

## 1．【学生】作業前に準備するもの

- Gitがインストールされたパソコン
- GitHubアカウント
- 研究室リポジトリへのアクセス権
- 自分が担当する企画コード

Gitを利用できるか、ターミナルまたはPowerShellで確認します。

```bash
git --version
```

バージョンが表示されれば利用できます。コマンドが見つからない場合はGitをインストールしてください。

## 2．【学生】研究室リポジトリをcloneする

コードを置きたい場所で、次のコマンドを実行します。

```bash
git clone https://github.com/murata-lab/unilab_2026.git
cd unilab_2026
```

cloneとは、GitHubにある研究室リポジトリを自分のパソコンへコピーする操作です。

次のコマンドで、正しい場所にいることを確認します。

```bash
git rev-parse --show-toplevel
git status
```

表示されたパスの末尾が `unilab_2026` なら問題ありません。

## 3．【学生】自分の作業用ブランチを作る

コードを入れる前に、企画ごとの作業用ブランチを作ります。ブランチ名は `add/企画ディレクトリ名` としてください。

```bash
git switch -c add/企画ディレクトリ名
```

例：

```bash
git switch -c add/asr-suika
```

次のコマンドで、今いるブランチを確認できます。

```bash
git branch --show-current
```

`main` ではなく、作成した `add/企画ディレクトリ名` が表示されれば問題ありません。

## 4．【学生】自分の企画コードを入れる

cloneした `unilab_2026` をFinderまたはエクスプローラーで開きます。自分が担当した企画名のディレクトリを作り、その中へ完成済みコードをコピーしてください。

例：担当企画が `asr-suika` の場合

```text
unilab_2026/
└── asr-suika/
    ├── README.md
    ├── src/
    └── その他のコード
```

すでに同名の企画ディレクトリやファイルがある場合は、内容を確認せずに上書きしないでください。他の人が先に提出している可能性があるため、管理者へ確認してください。

## 5．【学生】不要なファイルが含まれていないか確認する

アップロード前に、企画ディレクトリ内の `.gitignore` を確認します。`.gitignore` は、GitHubへアップロードしないファイルを指定するためのファイルです。

通常は次のようなものをアップロードしません。

- パスワード、APIキー、認証情報
- `.env` など秘密情報を含む設定ファイル
- Pythonの `.venv/`、`__pycache__/`
- Node.jsの `node_modules/`
- ビルドで生成される `dist/`、`build/`
- macOSの `.DS_Store`
- 個人情報を含むファイル
- 容量の大きい動画、学習モデル、データセット

必要な除外項目を、企画ディレクトリ内の `.gitignore` に記載します。

例：

```gitignore
.env
.env.*
.venv/
__pycache__/
node_modules/
dist/
.DS_Store
```

企画によって必要なファイルは異なります。判断できないファイルは、アップロードする前に管理者へ確認してください。

## 6．【学生】アップロードする内容を確認する

`unilab_2026` の直下で次を実行します。

```bash
git status
```

自分の企画以外のディレクトリ、個人ファイル、秘密情報が表示されていないことを確認します。

問題がなければ、自分の企画ディレクトリだけを追加します。

```bash
git add 企画ディレクトリ名
```

例：

```bash
git add asr-suika
```

次のコマンドで、実際にコミットされるファイルを確認します。

```bash
git status
git diff --staged
```

関係ないファイルが含まれていた場合は、コミットせず管理者へ相談してください。

## 7．【学生】commitする

確認したファイルをcommitします。

```bash
git commit -m "Add 企画名 project"
```

例：

```bash
git commit -m "Add asr-suika project"
```

commitとは、変更内容をGitの履歴として保存する操作です。この時点では、まだGitHubへ送信されていません。

## 8．【学生】作業用ブランチをGitHubへpushする

commitした内容を、手順3で作った作業用ブランチへpushします。

```bash
git push -u origin add/企画ディレクトリ名
```

例：

```bash
git push -u origin add/asr-suika
```

`main` へ直接pushしないでください。複数人が同時に提出していても、企画ごとに異なるブランチ名とディレクトリ名を使えば、通常は互いの提出を上書きしません。

## 9．【学生】Pull Requestを作成する

1. ブラウザで研究室のGitHubリポジトリを開きます。
2. push後に表示される **Compare & pull request** を押します。
3. マージ先が `main`、提出元が `add/自分の企画名` になっていることを確認します。
4. タイトルに企画名、説明欄に企画概要・起動方法・注意事項を書きます。
5. **Create pull request** を押します。

Pull Requestを作った時点で、学生側の最初の提出操作は完了です。ただし、コードはまだ `main` には追加されていません。この後は管理者が内容を確認します。

管理者から修正を求められた場合に限り、学生は同じブランチで修正・commit・pushしてください。変更は同じPull Requestへ自動的に反映されます。

```bash
git add 企画ディレクトリ名
git commit -m "Fix 企画名 project"
git push
```

GitHubが競合（conflict）を表示した場合は、自分で無理に解消せず管理者へ連絡してください。

# 提出する学生への重要な注意

### 企画ディレクトリ内で `git init` しない

`unilab_2026` 全体が1つのGitリポジトリです。企画ディレクトリ内で次のコマンドを実行しないでください。

```bash
git init
git remote add origin ...
```

企画内に別のGitリポジトリを作ると、研究室リポジトリから中のコードを正しく管理できなくなります。

### `git add .` をなるべく使わない

関係ないファイルを誤って追加する可能性があります。次のように自分の企画ディレクトリを指定してください。

```bash
git add 企画ディレクトリ名
```

### `main` へ直接pushしない

各学生は `add/企画ディレクトリ名` ブランチへpushし、Pull Requestを作成してください。これにより、管理者が内容を確認してから `main` へ追加でき、複数人による同時提出も安全に処理できます。

### 秘密情報をアップロードしない

APIキーやパスワードを一度pushすると、ファイルを削除してもGitの履歴に残る可能性があります。誤ってpushした場合は、すぐにキーやパスワードを無効化し、管理者へ連絡してください。

### 強制的なGit操作をしない

分からない状態で次のような操作をしないでください。

```bash
git reset --hard
git push --force
```

他の人の変更や自分のコードを失う可能性があります。

## 【学生】Pull Request作成前の最終チェック

- 正しい `unilab_2026` をcloneした
- `add/企画ディレクトリ名` ブランチを作成した
- 自分の企画名のディレクトリへコードを入れた
- 企画ディレクトリ内で `git init` していない
- `.gitignore` を確認した
- APIキー、パスワード、個人情報を含んでいない
- `node_modules/` や `.venv/` などを含んでいない
- `git status` で自分の企画以外が入っていないことを確認した
- `git diff --staged` でcommit内容を確認した
- `main` ではなく自分の作業用ブランチへpushした
- GitHub上でPull Requestを作成した

# 管理者が行う操作

ここからはリポジトリ管理者向けです。提出する学生は操作する必要がありません。

## 10．【管理者】Pull Requestを確認する

GitHubで学生のPull Requestを開き、次を確認します。

- 提出元が学生の `add/企画名` ブランチになっている
- マージ先が `main` になっている
- 変更がその学生の企画ディレクトリ内に収まっている
- APIキー、パスワード、個人情報が含まれていない
- `.env`、`node_modules/`、`.venv/`、生成物などが含まれていない
- 不要に大きなファイルが含まれていない
- 他の企画や共通ファイルを意図せず変更していない
- GitHub上で競合が表示されていない

問題がある場合はmergeせず、Pull Requestのコメントで学生に修正を依頼します。

## 11．【管理者】`main` へmergeする

内容に問題がなければ、GitHub上の **Merge pull request** を押して `main` へ統合します。merge完了後、不要になった作業用ブランチは **Delete branch** で削除できます。

競合がある場合や内容を判断できない場合は、無理にmergeしないでください。関係する学生へ確認してから対応します。

## 【管理者】merge前の最終チェック

- Pull Requestのマージ先が `main` になっている
- 変更対象が提出者の企画ディレクトリだけになっている
- 秘密情報、個人情報、不要な生成物が含まれていない
- 競合が発生していない
- 必要な修正がすべて反映されている
- 確認が終わるまでmergeしていない

## 困ったとき

無理に操作を続けず、次の情報を管理者へ送ってください。

```bash
git status
git rev-parse --show-toplevel
git branch --show-current
git remote -v
```

あわせて、実行したコマンドとエラーメッセージ全文を共有してください。パスワードやAPIキーが表示されている場合は、その部分を隠してください。
